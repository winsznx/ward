{
  "name": "ward-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ward orchestrator spine — INTAKE→…→SETTLE token-DD FSM with a firewall-gated single-supplier fan-out, settling real orders on Base mainnet",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "ward": "tsx src/index.ts",
    "provider": "tsx src/provider.ts",
    "probe": "tsx src/probe.ts",
    "start": "tsx src/index.ts",
    "test:offline": "tsx verify-offline.itest.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@croo-network/sdk": "0.2.1",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.0",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0"
  }
}
